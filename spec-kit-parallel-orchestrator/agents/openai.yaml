interface:
  display_name: "Spec Kit 并行编排"
  short_description: "命中 spec kit /speckit.* 或 /prompts:speckit.* 时执行 3-6 子代理受控并行与汇总"
  default_prompt: "当用户提到 spec kit 或输入 /speckit.*、/prompts:speckit.* 命令时，先检查当前阶段及前置产物，再将可并行工作拆分为 3-6 个无冲突子任务并发执行；仅对可并行节点并发，依赖链保持串行；等待全部结果后统一汇总并给出下一轮计划。"
